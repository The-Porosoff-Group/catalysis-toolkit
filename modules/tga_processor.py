"""
modules/tga_processor.py
Placeholder for TGA / TPR / TPO data processing module.
─────────────────────────────────────────────────────────
To implement: add a run(filepath, output_dir, metadata, params) function
that returns a result dict with plot_path, summary_path, etc.
The app.py will automatically detect this module and add a tab for it.
"""

MODULE_INFO = {
    'name':        'TGA / TPR / TPO',
    'description': 'Thermogravimetric & temperature-programmed analysis',
    'status':      'coming_soon',
    'icon':        '🌡️',
}

def run(filepath, output_dir, metadata, params):
    raise NotImplementedError("TGA module not yet implemented.")
